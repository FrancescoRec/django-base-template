# Django Settings
DJANGO_SETTINGS_MODULE=config.settings.local
SECRET_KEY=change-me-in-production-please-generate-a-new-secret-key
DEBUG=True

# Database Configuration (PostgreSQL)
DB_NAME=myproject
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# For local development, you can use SQLite instead
USE_SQLITE=false

# Production settings
ALLOWED_HOSTS=127.0.0.1,localhost

# Docker environment
DJANGO_ENV=local
